#include <iostream>

using namespace std;

int main()
{
    /* print the output to stdout */
    int a, b, c;
    /* get the input from stdin */
    cin >> a >> b >> c;
    cout << a + b + c << endl;

    /* for n number of inputs: while (cin>n) sum+=n */

    return 0;
}